<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skypapaya.mapper.InteractionMapper">

    <select id="checkStatus" resultType="int">
        SELECT COUNT(*) FROM interaction
        WHERE user_id = #{userId}
          AND target_id = #{targetId}
          AND type = #{type}
          AND target_type = #{targetType}
    </select>

    <insert id="insertInteraction">
        INSERT INTO interaction (user_id, target_type, target_id, type)
        VALUES (#{userId}, #{targetType}, #{targetId}, #{type})
    </insert>

    <delete id="deleteInteraction">
        DELETE FROM interaction
        WHERE user_id = #{userId} AND target_id = #{targetId} AND type = #{type} AND target_type = #{targetType}
    </delete>
</mapper>
