<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skypapaya.mapper.FollowMapper">

    <select id="checkFollow" resultType="int">
        SELECT COUNT(*) FROM follow WHERE follower_id = #{followerId} AND followee_id = #{followeeId}

    </select>

    <insert id="insertFollow">
        INSERT INTO follow (follower_id,followee_id,create_time)
        VALUES(#{followerId},#{followeeId},now())

    </insert>

    <delete id="deleteFollow">
        DELETE FROM follow WHERE follower_id = #{followerId} AND followee_id = #{followeeId}

    </delete>

    <select id="countFollowee" resultType="int">
        SELECT COUNT(*) FROM follow WHERE follower_id = #{userId}

    </select>

    <select id="countFollower" resultType="int">
        SELECT COUNT(*) FROM follow WHERE followee_id = #{userId}

    </select>

</mapper>